

#include "../h/scheduler.h"
void scheduler(){
	if(currentProc != NULL){


		currentProc = removeProcQ(); /* get who is next*/
		/*
		if not empty
			store a value on the timer
			*/
		/*length of a quantom
		when at 0 call an interupt*/
		LoadState /*privaledged instruction*/

		/*loads all 35 registers*/
		LDST = (& of a processor state) inside the PCB
		LDST(&(curentProc -> p_s));
		/*if empty
		....
		*/
	}
	
	pcb_PTR nextProc = removeProcQ(&
	
	if(nextProc != NULL){
		currentProc = nextProc;
	}else{
	
	}
}
